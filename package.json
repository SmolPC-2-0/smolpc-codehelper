{
	"name": "smolpc-platform-migration",
	"private": true,
	"version": "1.0.0",
	"workspaces": [
		"app"
	],
	"scripts": {
		"dev": "npm run dev --workspace app",
		"build": "npm run build --workspace app",
		"preview": "npm run preview --workspace app",
		"check": "npm run check --workspace app",
		"format": "npm run format --workspace app",
		"lint": "npm run lint --workspace app",
		"tauri": "npm run tauri --workspace app",
		"tauri:dev": "npm run tauri:dev --workspace app",
		"tauri:dml": "npm run tauri:dml --workspace app",
		"runtime:setup:openvino": "npm run runtime:setup:openvino --workspace app",
		"runtime:setup:python": "npm run runtime:setup:python --workspace app",
		"model:setup:qwen25-instruct": "npm run model:setup:qwen25-instruct --workspace app",
		"model:setup:qwen3-4b": "npm run model:setup:qwen3-4b --workspace app",
		"boundary:check": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/check-boundaries.ps1"
	}
}
